// To avoid name clashes with exposed contexts we use an object literal which is
// guaranteed to be unique. This key is in a different file to avoid circular
// import issues.
const key = {};

export default key;
